String Methods 
new String()
replce()
repeat()
include() true if presenty in a String 
indexof()
lastIndexOf()    from last  value
slice() copy the uindex vakue 
string[10] return item on 10 index 
split("") convert to a string to array
join(",")  make a array to string  wird in between will be attach to all items


Date 
nee date() 
getTime(),
getSeconds();
getDay();
all methods return the Time 



ARRAY  
new Array(as) new array created
     sort() arange in alpabativcaly Order  
     shift() remove first item from an array
     pop() remove last item from an array
     slice() remove series of item  pass as Index Value 
     delete() remove item from an array Not Lenghth
     push() add item at the last of array
     splice(0) remove all item from an array delet them
     findindex() return index value 
     find() return  value we search 
     split("") creat a array of charters split them
     [...string] same as above
     filter() filter item we wnat and new array
        reverse() array 
        Array.isArray() check it is an array
        REDUCER()  IS alos one of main Methods used to manuplate the item  for filter item 
        map() return all key and values from an array 
        sort() it also arrange the number we have to defina callBack inline function
        join() join all elemt of array
        entry() return an array with Kay and Value like apir
        

        example
        const even=(lo)=>[lo][0].filter(n=>n%2===0);



        CONCATINATING ARRAY   add item of array 
        a=[],b=[]
        c=[...a,...b]

        ARRAY.form() convert array like Object in a array appply all array methods on it 


REST AND SPREAD operators [...as], [a,d[...azz]]

        LOOPS 
    itterate item from an Objext and Array    
FOR loop simple one like C,
for in loop
for of loop

        DESTRUCTURE arange


        {% comment %} object {% endcomment %}
        OBJECT  
         its methods and Properties we can use to manuplate them
        {... kok}=Object  creat new Object will be copy
        Object.freeze(object) we cant chnage valueas and add new only read them
        Object.assign(objet ) can append e4xisting can also creat a new withh al object pass in Properties
        {...lkl,...loo}= add two Obeject with each other
        get and set in an Object 
        can also pass function as a poroperty 
        for in loop , and  Object.key().map() get al values 
        Object.keys(object) //return the keys 
        Object.values(object)
         we can also defin Properties on values as we wnat only read don,t edit  or run a function on getting the value


         MATH object